# ADR 010: Versioned machine-readable output

**Status:** Accepted

JSON schema 1.0 exposes the complete evidence-linked session and graph. Stable IDs and deterministic ordering let CI, IDEs, coding agents, and future MCP tools consume output without parsing terminal prose.
