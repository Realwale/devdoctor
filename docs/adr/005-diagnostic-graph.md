# ADR 005: In-memory diagnostic graph

**Status:** Accepted

A typed immutable multigraph is the canonical explanation and persists in session JSON. No graph database is used until measured scale or query needs justify its operational cost.
